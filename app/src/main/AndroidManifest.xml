<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.airshare.app">

    <!-- Zero Internet Permission - 100% Local Peer-to-Peer -->
    <!-- Local Wi-Fi & P2P -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Android 13+ Nearby Devices for Wi-Fi Direct -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="tiramisu" />

    <!-- Coarse/Fine Location for Wi-Fi Direct discovery on Android 12 and below -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Local Media & Storage Access -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />

    <!-- Android 13+ Granular Media Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Camera for QR Code Pairing -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Query Installed Applications for App/APK Sharing -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- 1-Tap Install APK after receiving -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Foreground Transfer Service and WakeLock -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Queries for Launchable and Installed Apps -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

    <application
        android:name=".AirShareApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@null"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AirShare"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Splash / Main Hub -->
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false" />

        <!-- Send Flow -->
        <activity
            android:name=".ui.send.SendActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <!-- Receive Flow -->
        <activity
            android:name=".ui.receive.ReceiveActivity"
            android:exported="false" />

        <!-- Group Broadcast Flow -->
        <activity
            android:name=".ui.group.GroupActivity"
            android:exported="false" />

        <!-- Web / PC Connect -->
        <activity
            android:name=".ui.webconnect.WebConnectActivity"
            android:exported="false" />

        <!-- Phone Clone -->
        <activity
            android:name=".ui.clone.PhoneCloneActivity"
            android:exported="false" />

        <!-- Transfer Progress -->
        <activity
            android:name=".ui.transfer.TransferProgressActivity"
            android:exported="false" />

        <!-- Camera QR Scanner -->
        <activity
            android:name=".ui.qr.QrScannerActivity"
            android:exported="false" />

        <!-- Foreground Service for Non-Interrupted Background Transfers -->
        <service
            android:name=".service.TransferForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- FileProvider for Sharing and Viewing Received Files & Installing APKs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
