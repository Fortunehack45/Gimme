<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Card.AirShare"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:clickable="true"
    android:focusable="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp">

        <ImageView
            android:id="@+id/ivFileThumbnail"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_pill_badge"
            android:padding="8dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_category_file"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvSelectableFileName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="middle"
            android:singleLine="true"
            android:text="MyFile.pdf"
            android:textColor="?attr/colorOnSurface"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/cbFileSelect"
            app:layout_constraintStart_toEndOf="@id/ivFileThumbnail"
            app:layout_constraintTop_toTopOf="@id/ivFileThumbnail" />

        <TextView
            android:id="@+id/tvSelectableFileSize"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            android:text="4.2 MB"
            android:textColor="@color/text_dark_secondary"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="@id/ivFileThumbnail"
            app:layout_constraintEnd_toStartOf="@id/cbFileSelect"
            app:layout_constraintStart_toEndOf="@id/ivFileThumbnail"
            app:layout_constraintTop_toBottomOf="@id/tvSelectableFileName" />

        <com.google.android.material.checkbox.MaterialCheckBox
            android:id="@+id/cbFileSelect"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:clickable="false"
            app:buttonTint="@color/brand_primary"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
